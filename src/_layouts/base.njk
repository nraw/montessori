<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% if title %}{{ title }} - {% endif %}The Montessori Method</title>
    <link rel="stylesheet" href="{{ '/css/style.css' | url }}">
</head>
<body>
    <header>
        <div class="container">
            <h1>The Montessori Method</h1>
            <p>Chapter Summaries & Key Insights from Maria Montessori</p>
        </div>
    </header>

    <nav>
        <div class="container">
            <ul>
                <li><a href="{{ '/' | url }}">Home</a></li>
                <li><a href="{{ '/summaries/Master_Summary/' | url }}">{{ 'Master_Summary' | navTitle }}</a></li>
                {% for summary in collections.summaries %}
                    {% if summary.fileSlug != 'Master_Summary' %}
                    <li><a href="{{ summary.url | url }}" title="{{ summary.fileSlug | chapterTitle }}">{{ summary.fileSlug | navTitle }}</a></li>
                    {% endif %}
                {% endfor %}
            </ul>
        </div>
    </nav>

    <main>
        <div class="container">
            {{ content | safe }}
        </div>
    </main>

    <footer>
        <div class="container">
            <p>&copy; 2024 The Montessori Method - Chapter Summaries. Original work by Maria Montessori.</p>
            <p>Summaries created to explore revolutionary educational principles.</p>
        </div>
    </footer>
</body>
</html>